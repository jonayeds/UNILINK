
import { useParams } from "react-router-dom";

const PostDetails = () => {
    const param = useParams()
    console.log(param)
    return (
        <div>
            
        </div>
    );
};

export default PostDetails;